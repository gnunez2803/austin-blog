import './NoPage.css';

function NoPage() {
      return (
        <div>
          <h1 className='title'>You are trying to access a page that does not exist.</h1>
        </div>
      );
}

export default NoPage;